# Tiempo_Santiago

Simple web app that shows the **hourly weather projection for Santiago, Chile**.

It includes charts for:
- Temperature
- Humidity
- Rain probability

## Run locally

From the repository root:

```bash
python3 -m http.server 8000
```

Then open:

- http://localhost:8000/index.html
